# Your Google OAuth Client ID (same one used in the backend)
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Backend base URL — in dev the Vite proxy handles this, so leave blank or set for production
# VITE_API_BASE_URL=https://api.yourcompany.com
